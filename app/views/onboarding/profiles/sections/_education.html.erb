<section class="rounded-lg border border-slate-200 bg-white p-6 shadow-sm"
         data-controller="nested-form"
         data-nested-form-association-value="educations">
  <div class="flex items-center justify-between">
    <h2 class="text-base font-semibold text-slate-900">Education</h2>
    <button type="button" data-action="nested-form#add"
            class="rounded-md border border-slate-300 px-3 py-1.5 text-sm font-medium text-slate-700 hover:bg-slate-50">
      Add education
    </button>
  </div>

  <template data-nested-form-target="template">
    <%= form.fields_for :educations, Education.new, child_index: "NEW_RECORD" do |ef| %>
      <%= render "onboarding/profiles/sections/education_fields", ef: ef %>
    <% end %>
  </template>

  <div data-nested-form-target="target" class="mt-4 space-y-4">
    <%= form.fields_for :educations do |ef| %>
      <%= render "onboarding/profiles/sections/education_fields", ef: ef %>
    <% end %>
  </div>
</section>
