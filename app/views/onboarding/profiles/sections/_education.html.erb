<section class="section-card"
         data-controller="nested-form"
         data-nested-form-association-value="educations">
  <div class="flex items-center justify-between">
    <%= onboarding_section_header("Education", :education) %>
    <button type="button" data-action="nested-form#add" class="inline-flex items-center gap-1.5 rounded-lg border border-brand-200 bg-brand-50 px-3 py-1.5 text-sm font-medium text-brand-700 transition hover:bg-brand-100"><svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M5 12h14"></path></svg>Add education</button>
  </div>

  <template data-nested-form-target="template">
    <%= form.fields_for :educations, Education.new, child_index: "NEW_RECORD" do |ef| %>
      <%= render "onboarding/profiles/sections/education_fields", ef: ef %>
    <% end %>
  </template>

  <div data-nested-form-target="target" class="mt-4 space-y-4">
    <%= form.fields_for :educations do |ef| %>
      <%= render "onboarding/profiles/sections/education_fields", ef: ef %>
    <% end %>
  </div>
</section>
